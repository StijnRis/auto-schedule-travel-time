{
  "name": "auto-schedule-travel-time",
  "private": true,
  "description": "Tests for the Google Apps Script in src/ (runs the real .gs files against fake Google services)",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
