/**
 * Personal configuration (optional).
 *
 * Copy this file to src/Config.local.gs (git-ignored) and add it to your Apps
 * Script project as a file named Config.local. Anything set here overrides
 * Config.gs; nested objects (MODE_SELECTION, NS) are merged, arrays are replaced.
 */
const LOCAL_CONFIG = {
  TARGET_CALENDAR_ID: 'your-travel-calendar-id@group.calendar.google.com',
  HOME_LOCATION: 'Your Street 1, Your City, Country',
  NS_API_KEY: ''
};
